/* ==========================================================================
   EXTERNAL CONFIGURATION — API keys are NOT stored here.
   Auth is handled server-side by the Cloudflare Worker proxy.
   ========================================================================== */
const CONFIG = {};
